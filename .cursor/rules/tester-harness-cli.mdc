---
description: Do not run tester harness CLIs that write results without user permission
alwaysApply: true
---

# Tester harness CLIs

## Permission before `--save-result`

Do not run artifact-writing harness CLIs unless the user asked in this chat, a fresh run is required for an explicit request, or you are debugging the harness itself.

Ask first before:

- `tester-v1/model_calibration.py --save-result` (three reference probes via `single_test_runner --save-result --quiet`; JSON under `results/{model}/{variant}/`; session file under `results/{model}/calibration-sessions/`)
- `tester-v1/single_test_runner.py --save-result` (JSON under `results/{model}/{variant}/`)

Same rule for `./model_calibration.py` / `./single_test_runner.py` from `tester-v1/`. Probe runs without `--save-result` are stdout-only and need no ask.

## Prefer existing local output

`tester-v1/results/` is gitignored. For docs, comparisons, or VRAM/context answers:

1. Use metrics already in repo README or other docs.
2. Else read newest `*.json` under `tester-v1/results/{model}/{variant}/`, or `calibration-sessions/{session_id}.json` for saved calibration.
3. Recompute from those files when needed (`idle_vram_mb`, calibration math).
4. Suggest or run a new harness only if local data is missing, stale, or the user wants a new run.

## Never use `configs/reference/` as `config_dir`

Do not pass `tester-v1/configs/reference/` (or a variant under it) as `config_dir` to either CLI. See `configs-reference.mdc`.
