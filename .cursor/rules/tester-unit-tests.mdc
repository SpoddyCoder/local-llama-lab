---
description: Unit tests must not write artifacts under tester-v1/results/
globs: tester-v1/tests/**/*.py
alwaysApply: false
---

# Tester unit tests

## No writes under `tester-v1/results/`

That tree is gitignored harness output only. Use inline dicts/strings or `tempfile` for fixtures and assertions.

Read-only use of `MODELS_ROOT / "..."` is fine.

## Mocked save-result paths

When `write_result` is mocked but code still writes siblings (e.g. `{stem}-output.txt` with `--include-output`), put the mocked path under `tempfile.TemporaryDirectory()`, not `_TESTER_ROOT / "results"`.

```python
with tempfile.TemporaryDirectory() as tmp:
    result_path = Path(tmp) / "results" / model_slug / variant / "20260101T000000Z.json"
    patch("runner.write_result", return_value=result_path),
```

See `test_include_output_with_save_result_writes_output_txt` in `tester-v1/tests/test_runner_cli.py`.

Mocking `write_result` alone without sibling writes may use a path under `_TESTER_ROOT / "results"` if no real I/O occurs.
